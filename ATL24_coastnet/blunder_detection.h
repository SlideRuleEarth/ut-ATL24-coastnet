#pragma once

#include <vector>

namespace ATL24_coastnet
{

template<typename T>
std::vector<unsigned> blunder_detection (const T &p)
{
    std::vector<unsigned> q (p.size ());
    return q;
}

} // namespace ATL24_coastnet
